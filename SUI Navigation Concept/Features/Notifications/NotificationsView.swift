
import SwiftUI

struct NotificationsView: View {
    var body: some View {
        ZStack {
            Color.pink.padding().opacity(0.5)
            Text("Notifications Settings")
        }
    }
}

#Preview {
    NotificationsView()
}
